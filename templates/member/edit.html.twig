{% extends 'Admin/admin_template.html.twig' %}

{% block title %}Edit Member{% endblock %}
{% block title2 %}{% endblock %}

{% block customStyleSheet %}
    <link rel="stylesheet" type="text/css" href="{{ asset('asset/css/datetimepicker.css') }}" >
{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-sm-2"></div>
        <div class="col-xl-8 order-xl-1">
            <div class="card bg-secondary shadow">
                <div class="card-header bg-white border-0">
                    <div class="row align-items-center">
                        <div class="col-8">
                            <h1 class="mb-0">
                                <a href="{{ path('member_index') }}" class="fas fa-arrow-left text-primary"></a>
                                Modification des informations d'un membre
                            </h1>
                        </div>
                    </div>
                </div>
                <div class="card-body">
                    {{ include('member/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
                    <br/>
                    <div class="row">
                        <div class="col">
                            {{ include('member/_delete_form.html.twig') }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block customJavascript %}
    <script src="{{ asset('asset/js/DateTimePicker.js') }}"></script>
    <script type="text/javascript">
        $('#form_member').on('submit', function (event) {
            let day = document.getElementById("member_expired_at_day").value;
            let month = document.getElementById("member_expired_at_month").value;
            let year = document.getElementById("member_expired_at_year").value;
            alert(day + " " +month+" "+year);
            return false;
        })
    </script>
{% endblock %}


